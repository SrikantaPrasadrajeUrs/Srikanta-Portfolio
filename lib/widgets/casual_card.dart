import 'package:flutter/material.dart';

class CasualCard extends StatelessWidget {
  const CasualCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 300,
      width: 300,
      color: Colors.grey,
    );
  }
}
